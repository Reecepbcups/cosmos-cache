2) Round Robin multiple endpoints
List of endpoints in the .env file. Change to a JSON?
.gitignore env / json file. Require the cp thing + documents

3) Add docker / Akash support + instructions

4) Example nginx config

5) Save keys in HSETS? so RPC->all RPC calls. SOrt based off time maybe>?
6) Save long term requests to JSON / persistant storage for future calls?

native Coingecko support (RPC & REST? Probably just REST)